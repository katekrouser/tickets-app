# A14 — QA / Quality Engineering — Handoff

Date: 2026-07-07 · Owner: A14 · Phase 3 · Environment: no Docker/Postgres/browser (unit + audit runnable only).

## QA GATE VERDICT: **FAIL** (blocked)

The quality program is complete and the runnable portion is green, but the gate cannot pass while blockers stand.

### Why FAIL (blockers, with owners)
1. **[BLOCKER · A4/A5 + Dependency Gate]** Critical runtime CVE in `@fastify/jwt` → `fast-jwt` (JWT algorithm
   confusion + unknown `crit` acceptance) — the sole bearer-auth path (ADR-0004). Tech-audit **F1**; matches R2 **MF-1**.
2. **[BLOCKER · A9/A1 + Dependency Gate]** Stack diverges from the FROZEN TDR: Vitest major split (BE 4.1.10 / FE 2.1.9,
   **F2**), TypeScript drift (root 5.9.3 vs pin 5.6.x, **F3**), `@testing-library/react` undeclared in TDR (**F4**;
   matches R2 MF-3), no DOM test env (jsdom/happy-dom) so component tests can't execute (**F5**).
3. **[CONDITION · A2]** Integration, security, E2E, and component suites are **authored but not executed** here.
   They must run green in CI/Docker before the gate flips.

Non-blocking defects to disposition: R5 **M1** (concurrent-drag whole-list rollback, A12), R5 **L1** (board cache
invalidation, A12), R4 High deployment/secret finding (A2), unused `@dnd-kit/sortable` (**F6**, A9/A12).

## Suites delivered
- **Backend unit** (`backend/tests/unit/**`, 7 files / **38 tests RUN✓**): invariants — modified_at only-on-real-change,
  epic-team rule, createdBy, Argon2id password policy, token 24h/single-use + resend invalidation, JWT expiry/malformed,
  env fail-fast, error contract.
- **Backend integration + security** (`backend/tests/integration/**`, 6 files / 42 tests, **CI/DB-gated**): auth+email
  flow, teams/epics/tickets/comments CRUD + all status codes incl. 409, and executable security regressions
  (no-token/malformed/tampered/garbage JWT, SQLi, stored-XSS, enum validation, no hash leak).
- **Frontend unit + performance** (`frontend/tests/**`, **22 tests RUN✓** incl. existing filters): validation, labels,
  error-normalization, board pipeline at ≥200 tickets under budget.
- **Frontend component** (`frontend/tests/component/**`, **DOM-gated, self-skip**): DS primitives + board card + inert-XSS render.
- **E2E** (`e2e/**`, Playwright, **CI/stack**): full critical path (signup→verify via Mailhog→login→team→epic→ticket→
  comment→drag→logout) + drag persist/refresh/rollback, on chromium+firefox+edge.

Runnable status here: **60 tests green** (38 BE + 22 FE); 42 BE + 8 FE + 9 E2E authored & self-skipping/pending CI.

## Docs delivered (`docs/quality/qa/`)
`tech-audit.md` (verdict FAIL), `rtm.md` (no unmapped requirement; requirement audit = CONDITIONAL FAIL),
`manual-checklist.md` (46 cases, every clause), `test-strategy.md`, `test-plan.md`, `risk-matrix.md`,
`smoke.md`, `regression.md`, `critical-path.md`, `coverage-report.md`.

## ACTION ITEMS for other agents

### A4 (backend manifest/CI) — REQUIRED for the suites to run via `npm test`
- Set the backend test script to use the A14 config:
  `"test": "vitest run --config tests/vitest.config.ts"` (config lives in `backend/tests/`, A14-owned).
  Without this, default `vitest run` fails to resolve the `backend/src/*` path alias.
- Resolve tech-audit **F1** (bump `@fastify/jwt`/`fast-jwt` to a patched release; keep within the 9.x pin).

### A9 (frontend manifest) + A1 (TDR) — via Technology Change Request
- **F2**: align frontend `vitest` to `^4` (match BE); pin the Vitest major in the TDR.
- **F5/F4**: add a DOM test env (`jsdom` or `happy-dom`) + set the FE `test.environment`; add Testing Library + the
  DOM env to the TDR. Then the component suite (`frontend/tests/component/**`) unskips and runs.
- **F6**: remove unused `@dnd-kit/sortable` (or adopt it) and sync the TDR.
- (optional) add `@vitest/coverage-v8` via TCR to enable instrumented line-% reporting.

### A1 (@app/shared) — F3
- Tighten `@app/shared` TypeScript to `~5.6` so a single 5.6.x resolves (TDR pin).

### A2 (CI wiring) — I did NOT edit `.github/`; please wire:
- **Backend unit job:** `npm ci && npm test --workspace backend` (after A4's script change) — runs the 38 unit tests.
- **Integration/security job:** add a Postgres service + `DATABASE_URL`, run `npm run migrate` then
  `npm test --workspace backend` — the DB-gated suites activate automatically.
- **Frontend job:** `npm test --workspace frontend` (+ component suite once F5 lands).
- **E2E job:** `docker compose up --build -d`, then
  `E2E_BASE_URL=… MAILHOG_URL=… npx playwright test --config e2e/playwright.config.ts` (install browsers first).
- Replace the placeholder `test` job in `ci.yml` accordingly. E2E specs + Playwright config are in `e2e/` (A14-owned).

## Path to QA gate = PASS
Clear F1 (0 Critical/High runtime vulns), F2–F5 (stack == TDR + DOM env), run integration/security/E2E/component
green in CI/Docker, and disposition R5-M1/L1 + R4-High. Then `rtm.md` requirement audit and `tech-audit.md` flip to
PASS and the gate opens.
