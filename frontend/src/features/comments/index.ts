/**
 * @/features/comments — public surface of the comments feature (A13-owned).
 *
 * `CommentsPanel` is embedded by the tickets feature's details view. Both features are the single
 * A13 Phase-1 deliverable; this barrel is the comments feature's published entry point.
 */
export { CommentsPanel } from './CommentsPanel';
